# Rename this file to secrets.toml and fill in values.
# SEC requires a descriptive User-Agent (your name + email).
SEC_USER_AGENT = "Your Name your.email@example.com"

# Optional: free Groq API key from https://console.groq.com
# Without it, the AI panels render placeholders but charts still work.
GROQ_API_KEY = ""
